# Mô Hình TCP/IP

Mô hình **TCP/IP** cũng là một mô hình tham chiếu phân tầng, thường được gọi là **Bộ Giao Thức Internet (Internet Protocol Suite)**. Thuật ngữ **TCP/IP** là viết tắt của hai giao thức **Transmission Control Protocol (TCP)** và **Internet Protocol (IP)**. **IP** nằm trong **tầng mạng (Layer 3 - Network Layer)** và **TCP** nằm trong **tầng vận chuyển (Layer 4 - Transport Layer)** của mô hình tầng **OSI**.

![image](https://github.com/user-attachments/assets/db2e6bf9-501f-42a9-9da1-eb786ca644e5)

Với **TCP/IP**, mọi ứng dụng đều có thể truyền và trao đổi dữ liệu qua bất kỳ mạng nào, và không quan trọng bên nhận đang ở đâu. **IP** đảm bảo rằng gói dữ liệu đến được đích, còn **TCP** kiểm soát việc truyền dữ liệu và đảm bảo kết nối giữa luồng dữ liệu và ứng dụng. Sự khác biệt chính giữa **TCP/IP** và **OSI** là số lượng tầng, trong đó một số tầng đã được gộp lại với nhau.

![image](https://github.com/user-attachments/assets/cbbe1afd-dc67-48d0-a0ce-efa77e74945e)

Các nhiệm vụ quan trọng nhất của **TCP/IP** bao gồm:

![image](https://github.com/user-attachments/assets/19e7fd29-0d85-47b0-b90a-6beb9ca96c9d)
